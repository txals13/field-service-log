# Copy to .dev.vars (which git ignores) and fill in the real server key.
# .dev.vars is only read by `wrangler dev`; the deployed Worker uses the secret.
ENVIRONMENT = "development"
TRANSLATE_KEY = "the-server-key-restricted-to-cloud-translation"
